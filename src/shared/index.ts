/**
 * Shared module
 *
 * Contains common utilities, hooks, styles, and UI components that are
 * reused across multiple features and are not tied to a specific feature.
 */

export * from "./hooks";
import "./styles/index.css";
